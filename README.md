Generates random variations for Those Who Rule game.

The python file can be used to generate an output file, which can then be copy/pasted into a save.

The output defaults to the 3rd slot, change this to the slot of the save file.

To avoid the need for running the randomizer, some pregenerated outputs are included with different configurations.
